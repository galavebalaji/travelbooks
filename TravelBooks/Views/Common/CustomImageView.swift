//
//  UserAvatarImageView.swift
//  TravelBooks

import UIKit

var imageCache = [String: UIImage]()

class CustomImageView: UIImageView {
    
    var lastURLUsedToLoadImage: String?
    
    func loadImage(urlString: String, completionHandlar: ((UIImage?) -> Void)? = nil) {
        lastURLUsedToLoadImage = urlString
        
        self.image = nil
        
        if let cachedImage = imageCache[urlString] {
            self.image = cachedImage
            return
        }
        
        guard let url = URL(string: urlString) else { return }
        
        URLSession.shared.dataTask(with: url) { (data, response, err) in
            if let err = err {
                Logger.log(message: "Failed to fetch post image: \(err)", messageType: .debug)
                return
            }
            
            if url.absoluteString != self.lastURLUsedToLoadImage {
                return
            }
            
            guard let imageData = data else { return }
            
            let photoImage = UIImage(data: imageData)
            
            imageCache[url.absoluteString] = photoImage
            
            DispatchQueue.main.async {
                if completionHandlar != nil {
                    completionHandlar?(photoImage)
                }else {
                    self.image = photoImage
                }
            }
            
            }.resume()
    }
}
